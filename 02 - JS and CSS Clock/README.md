# Day2 - JS时钟
用css 和JS 实现时钟
## Get 到的技能
- `transform-origin` ： 动态效果的中心一般默认为几何中心，transform-origin可以改变效果的中心。当其为100%时，动态中心就移动到端点
- `transform`的动态改变 ： 
```javascript
   element.style.transform='rotate(' +ratotedegree+ 'deg)';
```
- `transaction`动态改变时间的方式 ：`element.style.transaction = 'all 0.5s'`
